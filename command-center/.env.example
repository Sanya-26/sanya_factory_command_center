# command-center env — prod values get set in Cloudflare Pages dashboard.

# ─── Supabase (factory project) ────────────────────────────────────────
VITE_SUPABASE_URL=https://jzppqxiprjsuvuyulbyj.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...   # anon JWT — safe to ship; RLS + ops_users gate access

# ─── Cross-app link: where non-admin users get bounced to ──────────────
# When someone without an ops_users row hits the admin shell, we bounce
# them to the customer onboarding origin (so they go through signup).
VITE_AUTH_PORTAL_URL=https://welcome.aubos.ai

# ─── DEV-ONLY (never set these in production Pages env) ────────────────
# VITE_ENABLE_DEV_BYPASS=1
# VITE_DEV_ADMIN_EMAIL=...
# VITE_DEV_ADMIN_PASSWORD=...
